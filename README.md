The Markdown Resume
===================

Instructions:

    sudo apt-get update
    sudo apt-get install -y pandoc context
    git clone https://github.com/toddbu/pandoc_resume
    cd pandoc_resume
    vi resume.md
    make

Requirements:

 * ConTeXt
 * pandoc
